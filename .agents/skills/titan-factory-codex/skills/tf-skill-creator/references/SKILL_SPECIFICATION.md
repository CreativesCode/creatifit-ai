# Codex skill format

Use the native skill-creator when available. A SKILL.md requires name and description in YAML plus scoped instructions. Optional references, scripts and assets should be linked and loaded only when relevant. Claude-specific frontmatter is not a Codex configuration mechanism.

Official guide: https://learn.chatgpt.com/docs/build-skills
